# The subsystem name must exist in the model
# The filename must have the extension `.svg` and be named with only [a-zA-Z0-9_] characters.
# At moment there is no subsystem SVG map for this model, however, it might be added with future model updates
@subsystem name	map name	filename